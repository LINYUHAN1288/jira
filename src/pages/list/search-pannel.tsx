import { Form, Input } from "antd";

export const SearchPannel = () => {
    return (
        <Form style={{ marginBottom: "2rem" }} layout={"inline"}>
            <Form.Item></Form.Item>
        </Form>
    );
};
